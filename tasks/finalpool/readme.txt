# Final Pool

This directory contains the implemented tasks from the BenchTasksCollv3 project,
collected into a final pool. Each task subdirectory follows the standard task
layout (docs, evaluation, groundtruth_workspace, initial_workspace, preprocess).

Tasks in this pool are deemed implemented: they contain the complete task
structure required by the project (docs/, evaluation/, groundtruth_workspace/,
initial_workspace/, preprocess/ with non-empty, all-English documentation).

Implemented tasks (21):
